import React from "react";
import { Box } from "@mui/material";
import { HeroStyles } from "./Hero.styles";

export const Hero: React.FC = () => {

  const items = [
    {
      name: "paracas",

    }

  ]

  return <Box sx={HeroStyles.wrapper}>

  </Box>;
};
