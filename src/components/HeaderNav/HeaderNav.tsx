import { Box } from "@mui/material";
import { HeaderNavStyles } from "./HeaderNav.styles";

export const HeaderNav = () => {
  return (
    <header>
      <Box sx={HeaderNavStyles.wrapper}></Box>
    </header>
  );
};
